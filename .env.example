# 백엔드(HTTP API) 설정 템플릿 — 복사해서 쓰십시오:  cp .env.example .env.dev
#
# ⚠️ `.env.dev` 는 커밋되지 않습니다(.gitignore). 실제 접속정보·키를 이 템플릿 파일에 적지 마십시오.
# ⚠️ 값은 전부 placeholder 입니다. 자기 환경 값으로 바꿔야 동작합니다.
#
# 이 파일은 **서빙(검색·조회 API)에 필요한 값만** 담습니다. 적재 전용 값(텍스트 청킹·요약 길이·키워드 수·
# 파일 인코딩 · 수집 경로)은 백엔드가 읽지 않으므로 없습니다 — 백엔드는 코어 설정을 **서빙 역할**로
# 초기화해 그 값들을 요구하지 않습니다(093 5단계). 적재 설정은 파이프라인 레포의 .env.example 을 보십시오.
#
# 설정을 주는 방법은 두 가지입니다(README §환경변수 참조):
#   A) 이 파일(`.env.dev`) — 실행하는 디렉터리 또는 레포 루트에 두면 읽힙니다.
#   B) 환경변수 직접 주입 — 배포·컨테이너·CI. 설치 방식과 무관하게 항상 동작하고 A 보다 우선합니다.

# ── 🔴 필수 — 없으면 기동 시점에 ValueError 로 중단됩니다 ─────────────────────
META_MODEL=                                 # 온프레미스 LLM 모델 이름(검색 결과 검증·질의 정규화 LLM 방식·개체 판정이 켜져 있을 때 사용)
OPENAI_BASE_URL=http://localhost:8000/v1    # ⚠️ OpenAI 호환 **온프레미스** 엔드포인트(외부 서비스 아님)
OPENAI_API_KEY=changeme                     # 위 엔드포인트용. 온프레미스면 임의값도 가능
TEXT_EMBED_MODEL=                           # 텍스트 임베딩 모델(1536D 고정) — 질의를 벡터로 바꿀 때 씁니다(문서 색인과 같아야 합니다)
TEXT_EMBED_CHUNK_SIZE=512
TEXT_EMBED_NORMALIZE=true

# ── PostgreSQL 17 + pgvector ──────────────────────────────────────────────
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=
POSTGRES_USER=
POSTGRES_PASSWORD=

# ── OpenSearch (analysis-nori 필요 · 검색은 전부 여기서) ─────────────────────
OPENSEARCH_URL=http://localhost:9200
OPENSEARCH_INDEX=assets
OPENSEARCH_SYNC_ENABLED=true                # 백엔드는 색인하지 않지만, 코어 설정 정합 검사("OS 검색 ⇒ OS 색인 on")를 위해 true 로 둡니다

# ── 백엔드 실행 ───────────────────────────────────────────────────────────
PORTAL_API_ENV=dev                          # 읽을 .env 접미사(dev → .env.dev)
# THUMBNAIL_CACHE_DIR=                       # 썸네일 캐시 디렉터리(기본: 시스템 임시 디렉터리). 원본 파일 위치는 DB 의 fs_path 를 읽습니다

# ── 백엔드 인증 ───────────────────────────────────────────────────────────
PORTAL_AUTH_DISABLED=1                      # 1=연구·개발(토큰 없이 호출) · 0=운영(Bearer 필수)
# PORTAL_JWT_SECRET=                         # PORTAL_AUTH_DISABLED=0 이면 **필수**(없으면 기동 실패)
# PORTAL_JWT_ISSUER=                         # 설정 시 iss 핀(다른 서비스 토큰 재사용 차단)
# PORTAL_JWT_TTL_SECONDS=3600
# PORTAL_AUTH_BACKEND=local_hs256
